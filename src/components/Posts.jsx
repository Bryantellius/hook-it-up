import React from 'react';

const Posts = (user, posts) => {
    console.log(user.id);
    console.log(posts.arr);
    return (
        <React.Fragment>
            
            )}
        </React.Fragment>
    )
}

export default Posts;